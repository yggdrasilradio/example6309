need to go through all the colors, eliminate duplicates

needs one more color to replace the duplicate ORANGE

with more colors, sprites can have more realistic shading/colors

joystick doesn't respond quite right, needs more resolution

parameterize animation speeds

line routines can be optimized further

arbitrary line routine

