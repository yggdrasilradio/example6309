
explosiion doesn't recognize end of sprites marker

dots not right

joystick doesn't respond quite right

parameterize animation speeds

line routines can be optimized further

arbitrary line routine

with more colors, sprites can have more realistic shading/colors
